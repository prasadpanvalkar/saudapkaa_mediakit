version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: saudapakka_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: saudapakka_db
      POSTGRES_USER: hello_django
      # 🔐 CHANGED: New Strong Password
      POSTGRES_PASSWORD: "Secure_Password_SaudaPakka_2026!"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # 🚨 SECURITY FIX: "ports" section REMOVED. Database is now hidden from the internet.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hello_django -d saudapakka_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./saudapakka_backend
    container_name: saudapakka_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./saudapakka_backend/src:/app/src
      - ./saudapakka_backend/entrypoint.sh:/app/entrypoint.sh
      - properties_data:/app/properties
      - media_data:/app/media
    environment:
      - POSTGRES_DB=saudapakka_db
      - POSTGRES_USER=hello_django
      # 🔐 CHANGED: Must match the postgres service above
      - POSTGRES_PASSWORD=Secure_Password_SaudaPakka_2026!
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      # 🔐 CHANGED: Updated URL with new password
      - DATABASE_URL=postgresql://hello_django:Secure_Password_SaudaPakka_2026!@postgres:5432/saudapakka_db
      - SECRET_KEY=^_npf^&k0bp+e&k7gv$$n-wo03-bcqave^kjyxob$$utv3os%3_a
      - DEBUG=False
      - ALLOWED_HOSTS=saudapakka.com,72.61.246.159,localhost,127.0.0.1
      - CORS_ALLOWED_ORIGINS=https://saudapakka.com
      - EMAIL_HOST=smtp.gmail.com
      - EMAIL_PORT=587
      - EMAIL_USE_TLS=True
      # 🛠️ FIXED: Removed markdown syntax error
      - EMAIL_HOST_USER=qutubahmad3@gmail.com
      - EMAIL_HOST_PASSWORD=zpjkdugjokwjoicg
      - DEFAULT_FROM_EMAIL=SaudaPakka <qutubahmad3@gmail.com>
      - SANDBOX_API_KEY=key_live_9d0262d5524447b8b3b804a42481408f
      - SANDBOX_API_SECRET=secret_live_435d5ad9adfc4cce986a47a8d3af5d56
      - SANDBOX_BASE_URL=https://api.sandbox.co.in
      - SANDBOX_ENV=production
    depends_on:
      postgres:
        condition: service_healthy
    working_dir: /app
    command: >
      sh -c "chmod +x /app/entrypoint.sh && /app/entrypoint.sh"

  frontend:
    build: ./saudapakka_frontend
    container_name: saudapakka_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./saudapakka_frontend/env.production.template

  nginx:
    image: nginx:alpine
    container_name: saudapakka_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
      - properties_data:/app/properties
      - media_data:/app/media
    depends_on:
      - backend
      - frontend

  certbot:
    image: certbot/certbot
    container_name: saudapakka_certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot

volumes:
  postgres_data:
  properties_data:
  media_data:
